# ADR-[NNN]: [TITLE]

<!--
  HOW TO USE THIS TEMPLATE
  - This is the default structure for Architecture Decision Records produced by
    `speckit.spectra.adr`. Fill every [PLACEHOLDER].
  - Do not add, rename, or reorder sections when filling it. If your project needs a
    different structure, that is what the override is for — see below.
  - HTML comments like this one are guidance and MUST be deleted as you fill each
    section. No [PLACEHOLDER] token may survive into the finished ADR.

  CUSTOMIZING THIS TEMPLATE (per project, permanent, team-wide)
  - Copy this file to `.specify/templates/overrides/adr-template.md` and edit it there.
  - Commit it. Every ADR the agent writes from then on follows your structure, on every
    teammate's machine, and the override survives `specify extension update`.
  - Add sections your governance needs (Alternatives Considered, Compliance Impact,
    Drivers), or delete ones you do not want — sections you remove stay removed; the
    command will note the omission rather than adding them back.
  - Do NOT edit this file in place once installed under `.specify/extensions/`: the next
    extension update replaces the extension's files and your edit is lost.
-->

**Date:** [YYYY-MM-DD]

**Status:** [Proposed | Accepted | Deprecated | Superseded]

<!--
  Status: a new ADR is normally `Proposed`. Keep only the chosen value on the line —
  delete the alternatives. When a later ADR supersedes this one, this line becomes
  `Superseded` with a reference to the ADR that replaced it.
-->

## Context

<!--
  What is the issue or problem that motivates this decision? Ground it in this project:
  the modules, constraints, prior ADRs, and constitution principles actually involved.
  Capture the answers to the clarifying questions here. If this decision supersedes an
  earlier ADR, say so and name it.
-->

[Context]

## Decision

<!--
  What is the change being proposed or made? State it concretely and unambiguously —
  a reader should be able to tell whether a future change complies with it.
-->

[Decision]

## Consequences

<!--
  What becomes easier or harder as a result? Be honest about both: the benefits and the
  trade-offs, migration cost, and anything this closes off.
-->

[Consequences]
