# [PR TITLE]

<!--
  HOW TO USE THIS TEMPLATE
  - This is the structure `speckit.spectra.create-pr` fills when it opens a pull request.
    Fill every section. If one genuinely does not apply, write "N/A" rather than leaving it
    empty — a blank section reads as an oversight.
  - Guidance lives in HTML comments like this one. It does not render on GitHub, but it MUST
    still be deleted as each section is filled, along with every [PLACEHOLDER] token.
  - Write for a reviewer with no context on the ticket. They read the Summary first and often
    only the Summary.

  CUSTOMIZING THIS TEMPLATE (per project, permanent, team-wide)
  - Copy this file to `.specify/templates/overrides/pr-template.md` and edit it there.
  - Commit it. Every PR the command opens from then on follows your structure, on every
    teammate's machine, and the override survives `specify extension update`.
  - Add sections your process needs, or delete ones you do not want — sections you remove stay
    removed; the command follows your version and says what it left out rather than adding it
    back.
  - Do NOT edit this file in place once installed under `.specify/extensions/`: the next
    extension update replaces the extension's files and your edit is lost.
  - The heading above is the PR title, not a section. Keep it first.
-->

## Summary

<!--
  What does this change do, and why is it needed? Two to four sentences in plain language.
  Lead with the outcome, not the implementation.
-->

[Summary]

## Related Issues

<!--
  The issue this PR addresses, if there is one. Delete this section entirely when there is
  none — never write a placeholder issue number.

  A closing keyword (`Closes #123`) is only meaningful when the PR targets the repository's
  DEFAULT branch. GitHub ignores these keywords on any other base: no link is created and
  merging closes nothing. When targeting a non-default branch (a `dev` in a promotion flow,
  say), use a plain reference — `#123`, or the full URL for an issue in another repository —
  which still records a cross-reference on the issue.

  If you remove this section from your override, the pull request is still linked: the command
  appends a short Related Issues section rather than dropping an issue you passed with `--issue`.
  It will say that it did. Keep the section if you would rather choose where it sits.
-->

[Closes #NNN — or a plain reference, per the note above]

## Type of Change

<!-- Mark exactly one with [x]; delete the rest. -->

- [ ] Feature — new user-facing capability
- [ ] Bug fix — corrects broken behaviour
- [ ] Refactor — internal change, no behaviour change
- [ ] Documentation
- [ ] Build / CI / tooling
- [ ] Hotfix — urgent production fix

## Changes

<!--
  The notable changes, bulleted, so a reviewer knows where to look. Derived from the actual
  diff against the base branch — not from the plan or the ticket. Group by area when the diff
  is large, and skip trivia (formatting, import order).
-->

- [change]
- [change]

## How to Test

<!--
  Steps a reviewer can follow to verify this themselves: setup, the exact commands, and what
  they should observe. Name the test suite or specific tests when that is the fastest route to
  confidence.
-->

1. [step]
2. [step]

**Expected result:** [what the reviewer should see]

## Screenshots / Evidence

<!--
  Required for any user-facing change: before/after screenshots, a short recording, or terminal
  output. Command output pasted as a fenced block counts. Otherwise write "N/A".
-->

[Evidence, or N/A]

## Breaking Changes

- [ ] This PR contains breaking changes

<!--
  If checked, describe precisely what breaks — API contracts, config keys, database schema,
  public interfaces, CLI arguments — and the migration path for consumers. If unchecked,
  delete everything below the box.
-->

[What breaks, and the migration path]

## Notes for Reviewers

<!--
  Anything that helps the review: areas you are unsure about, trade-offs taken deliberately,
  work knowingly deferred to a follow-up, and parts of the diff that are safe to skim.
  Say so plainly when something was not verified.
-->

[Notes]
